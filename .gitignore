node_modules
.next
out
dist
build

# env — never commit. This is the mistake v1 shipped.
.env
.env.local
.env.*.local

.DS_Store
*.pem
npm-debug.log*
next-env.d.ts
tsconfig.tsbuildinfo

# Local SMTP sink for testing the enquiry mail path. Handy, not shipped.
smtp-catcher.mjs

# Cloudflare / OpenNext build output and local secrets
.open-next
.wrangler
.dev.vars
.dev.vars.*
# the template IS committed; the values never are
!.dev.vars.example
