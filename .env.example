# Copy to .env for local dev. The real values live in Cloudflare's
# environment variables for the deployed site -- never commit them.
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_KEY=<supabase anon public key -- the long eyJ... JWT>
